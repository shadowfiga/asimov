# Recommended next steps

1. Expand Graphite's `[UIElement]` adapters beyond `Text` and `Button` as real screens require them.
2. Add editor-time validation that every `[UIElement]` field resolves in its Gum screen.
3. Add `Engine/Rendering` with `Material`, `Shader`, `SpriteRenderer`, `Camera`.
4. Add MonoGame MGFX shader compilation.
5. Add UI material/shader support around Gum's rendering path.
6. Add `Assets.Load<T>()` caching.
7. Add serialization / `[Editable]` metadata only once a concrete editor need appears.
8. Add Tiled or LDtk for visual world/level editing rather than rebuilding a Unity scene editor.
