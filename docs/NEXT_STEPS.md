# Recommended next steps

1. Extract reusable code-first Myra widget factories and a Graphite stylesheet once the visual direction is established.
2. Add keyboard/gamepad focus navigation conventions for menus.
3. Add `Engine/Rendering` with `Material`, `Shader`, `SpriteRenderer`, `Camera`.
4. Add MonoGame MGFX shader compilation.
5. Add `Assets.Load<T>()` caching.
6. Add serialization / `[Editable]` metadata only once a concrete editor need appears.
7. Add Tiled or LDtk for visual world/level editing rather than rebuilding a Unity scene editor.
