# Legacy UI material

`GumProject` and `GumTooling` preserve the former Gum layouts and helper scripts for reference. They are not part of the Graphite runtime, are outside `Content`, and are not copied to build output.

New UI is authored directly in C# with Myra under `Game/UI`.
